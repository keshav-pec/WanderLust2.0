# Server Configuration
NODE_ENV=development
PORT=8080

# Database
# Get from MongoDB Atlas → Cluster → Connect → Connect your application
# Format: mongodb+srv://username:password@cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
ATLASDB_URL=your_mongodb_connection_string

# JWT Secret
# Generate with: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your_jwt_secret_key_change_in_production

# Cloudinary Configuration
# Get from Cloudinary Dashboard → Settings → Access Keys
CLOUD_NAME=your_cloudinary_cloud_name
CLOUD_API_KEY=your_cloudinary_api_key
CLOUD_API_SECRET=your_cloudinary_api_secret

# Frontend URL (for CORS)
# Local: http://localhost:5173
# Production: Multiple domains are hardcoded in server.js
# This is for custom domains only
FRONTEND_URL=http://localhost:5173
